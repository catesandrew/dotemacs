;;; keybindings.el --- Grammar keybindings -*- lexical-binding: t; -*-

;;; Commentary:

;; Grammar key bindings.

;;; Code:

(spacemacs/declare-prefix "og" "grammar")
(spacemacs/declare-prefix "oS" "spelling")

;;; keybindings.el ends here

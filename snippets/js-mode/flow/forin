# -*- coding: utf-8; mode: snippet; require-final-newline: nil -*-
# name: loop key/value object
# group: flow
# key: forin
# type: snippet
# --
for (var ${1:key} in ${2:source}) {
  if ({}.hasOwnProperty.call($2, $1)) {
    var ${3:val} = $2[$1]
    ${0}
  }
}

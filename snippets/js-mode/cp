# -*- mode: snippet; require-final-newline: nil -*-
# name: Creates and assigns a local variable using props destructing
# key: cp
# uuid: cp
# --
const { $1 } = this.props`(if (eolp) (cats/javascript-yasnippets-semicolon))`
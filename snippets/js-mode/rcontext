# -*- mode: snippet; require-final-newline: nil -*-
# name: Create React context
# key: rcontext
# uuid: rcontext
# --
const ${contextName} = React.createContext()`(if (eolp) (cats/javascript-yasnippets-semicolon))`